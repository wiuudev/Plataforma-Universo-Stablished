export const _setGeometricLayersVisibilityScale = (e, o, t, i) => { let l = o.map(e => e.type), m = e.filter(e => -1 !== l.indexOf(e.type)); t.on("zoomend", function () { for (let e of m) { let i = e.type, l = o.filter(e => e.type === i); if (1 === l.length) { let a = l[0], r = a.visibilityScales; r.minZoomLevel !== r.maxZoomLevel && (parseInt(t.getZoom()) > r.minZoomLevel && parseInt(t.getZoom()) <= r.maxZoomLevel ? t.hasLayer(e) || t.addLayer(e) : t.hasLayer(e) && t.removeLayer(e)) } } }) };